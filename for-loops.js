let numbers = [1, 2, 3, 4, 5, 6, 7];

for (let idx = 0; idx < numbers.length; idx++) {
    console.log(numbers[idx]);
    
} 
// 1
// 2
// 3
// 4
// 5
// 6
// 7


for (let num = 0; num < 100; num++) {
    console.log(num);
    
} 

// Loops
// For Loops: A for loop is use in order to execute a code block repeatedly. So rather than having to manually repeat that code which will lead to code duplication, you can define a code block defined within the curly braces and using the special syntax of the for-Loop you can continue to repeat those lines of code while the condition is true.
 
//syntax

// for (let index = 0; index < array.length; index++) {
//     const element = array[index];
    
// }
 
// syntax for a traditional for-loop
for (let idx = 0; idx < 100; idx++) {
    console.log(idx);
        
}